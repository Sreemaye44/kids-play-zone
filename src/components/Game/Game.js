import React from 'react';
import './Game.css'

const Game = (props) => {
    const {name, id,price,img}=props.game;
    
    return (
        <div>
        <h2>{name}</h2>
       
        
            
        </div>
    );
};

export default Game;